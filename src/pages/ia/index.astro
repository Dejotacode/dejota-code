---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostCard from '../../components/PostCard.astro';
import { getPublishedPosts } from '../../lib/editorial';
const posts = (await getPublishedPosts()).filter((post) => post.categorySlug === 'ia').sort((a,b)=>b.publishedAt.valueOf()-a.publishedAt.valueOf());
---
<BaseLayout title="Inteligência Artificial — Dejotacode" description="Ferramentas e aplicações práticas de IA para trabalho e negócios.">
  <section class="section"><div class="container">
    <span class="eyebrow">Categoria</span><h1>Inteligência Artificial</h1><p class="lead">Ferramentas e aplicações práticas de IA para trabalho e negócios.</p>
    <div class="grid grid-3">{posts.map((post) => <PostCard post={post} />)}</div>
    {posts.length === 0 && <div class="card"><h2>Conteúdo chegando.</h2><p class="muted">Ainda não há artigos publicados nesta categoria. Volte em breve.</p><a class="btn btn-primary" href="/blog/">Ver todos os artigos →</a></div>}
  </div></section>
</BaseLayout>
