# Cloudflare Pages build environment (example only; no real secrets).
# Server-side build source used by Astro to read published CMS content.
API_URL=https://api.dejotacode.com.br

# Public browser-facing API origin used by newsletter, briefing and feedback forms.
PUBLIC_API_URL=https://api.dejotacode.com.br
